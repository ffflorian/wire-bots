{
  "author": "Florian Keller <github@floriankeller.de>",
  "dependencies": {
    "@wireapp/bot-api": "7.1.18",
    "dotenv": "8.2.0",
    "logdown": "3.3.0",
    "moment": "2.24.0",
    "openweathermap-api-client": "1.0.3",
    "pm2": "4.2.1"
  },
  "devDependencies": {
    "@ffflorian/jszip-cli": "3.1.0",
    "@types/node": "~12",
    "cross-env": "6.0.3",
    "rimraf": "3.0.0",
    "typescript": "3.7.4"
  },
  "engines": {
    "node": ">= 10.9"
  },
  "license": "GPL-3.0",
  "main": "dist/index.js",
  "name": "@ffflorian/wire-weather-bot",
  "private": true,
  "repository": "https://github.com/ffflorian/wire-bots/tree/master/packages/wire-weather-bot",
  "scripts": {
    "build": "tsc",
    "clean": "rimraf dist",
    "deploy": "yarn dist && yarn zip && eb deploy -nh",
    "dist": "yarn clean && yarn build",
    "lint": "tslint -p tsconfig.json --fix",
    "start": "pm2 start && pm2 logs",
    "start:dev": "cross-env NODE_ENV=development node dist/index.js",
    "test": "yarn dist && exit 0",
    "zip": "jszip-cli"
  },
  "version": "1.0.0"
}
