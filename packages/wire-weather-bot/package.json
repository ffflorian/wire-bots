{
  "author": "Florian Keller <github@floriankeller.de>",
  "dependencies": {
    "@wireapp/bot-api": "1.0.8",
    "dotenv": "6.0.0",
    "logdown": "3.2.7",
    "moment": "2.22.2",
    "openweathermap-api-client": "1.0.3",
    "pm2": "3.1.2"
  },
  "devDependencies": {
    "@ffflorian/jszip-cli": "1.4.2",
    "@types/node": "10.10.1",
    "cross-env": "5.2.0",
    "prettier": "1.14.3",
    "rimraf": "2.6.2",
    "typescript": "3.0.3"
  },
  "license": "GPL-3.0",
  "main": "dist/index.js",
  "name": "@ffflorian/wire-weather-bot",
  "private": true,
  "repository": "https://github.com/ffflorian/wire-bots/tree/master/packages/wire-weather-bot",
  "scripts": {
    "build": "tsc",
    "clean": "rimraf dist",
    "deploy": "yarn dist && yarn zip && eb deploy -nh",
    "dist": "yarn clean && yarn build",
    "lint": "tslint -p tsconfig.json --fix",
    "prettier": "prettier --single-quote --write src/**/*.ts",
    "start": "pm2 start && pm2 logs",
    "start:dev": "cross-env NODE_ENV=\"development\" node dist/index.js",
    "test": "yarn dist && exit 0",
    "zip": "jszip-cli add dist .ebextensions package.json ecosystem.config.js -q -o dist/wire-weather-bot.zip"
  },
  "version": "1.0.0"
}
