{
  "author": "Florian Keller <github@floriankeller.de>",
  "dependencies": {
    "@wireapp/bot-api": "1.0.19",
    "dotenv": "6.0.0",
    "logdown": "3.2.7",
    "moment": "2.22.2",
    "pm2": "3.1.3"
  },
  "devDependencies": {
    "@ffflorian/jszip-cli": "2.1.0",
    "@types/node": "10.11.0",
    "@types/request": "2.47.1",
    "cross-env": "5.2.0",
    "prettier": "1.14.3",
    "rimraf": "2.6.2",
    "typescript": "3.0.3"
  },
  "license": "GPL-3.0",
  "main": "dist/index.js",
  "name": "@ffflorian/wire-echo-bot",
  "private": true,
  "repository": "https://github.com/ffflorian/wire-bots/tree/master/packages/wire-echo-bot",
  "scripts": {
    "build": "tsc",
    "clean": "rimraf dist",
    "deploy": "yarn dist && yarn zip && eb deploy -nh",
    "dist": "yarn clean && yarn build",
    "lint": "tslint -p tsconfig.json --fix",
    "prettier": "prettier --single-quote --write src/**/*.ts",
    "start": "pm2 start && pm2 logs",
    "start:dev": "cross-env NODE_ENV=\"development\" node dist/index.js",
    "test": "yarn dist && exit 0",
    "zip": "jszip-cli"
  },
  "version": "1.0.0"
}
