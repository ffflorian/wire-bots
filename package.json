{
  "author": "Florian Keller <github@floriankeller.de>",
  "devDependencies": {
    "lerna": "3.3.0"
  },
  "name": "wire-bots",
  "private": true,
  "scripts": {
    "boot": "lerna bootstrap --reject-cycles",
    "clean": "lerna run clean",
    "deploy": "lerna run deploy",
    "dist": "yarn clean && lerna run dist",
    "test": "yarn && yarn dist && lerna run test"
  },
  "workspaces": [
    "packages/*"
  ]
}
